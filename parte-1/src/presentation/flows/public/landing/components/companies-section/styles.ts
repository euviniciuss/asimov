import { cva } from "class-variance-authority";

export const heroSectionStyles = cva("app-container py-10 md:py-16");

export const heroContentStyles = cva("flex-1 max-w-[531px]");

export const heroImageStyles = cva("flex-1 flex justify-center");
